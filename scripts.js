// Function to display the hidden message
function showMessage() {
    var message = document.getElementById("specialMessage");

    // Remove the hidden class to display the message
    message.classList.remove("hidden");
}
